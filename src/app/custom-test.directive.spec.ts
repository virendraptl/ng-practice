import { CustomTestDirective } from './custom-test.directive';

describe('CustomTestDirective', () => {
  it('should create an instance', () => {
    const directive = new CustomTestDirective();
    expect(directive).toBeTruthy();
  });
});
